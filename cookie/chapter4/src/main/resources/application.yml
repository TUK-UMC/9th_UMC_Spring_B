spring:
  application:
    name: "chapter4"
jwt:
  token:
    secretKey: chapter4-secret-key-min-256-bits-long-secret-key-for-hs256-algorithm-should-be-very-long-string
    expiration:
      access: 3600000

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # MySQL JDBC ???? ??? ??
    url: ${DB_URL} # jdbc:mysql://localhost:3306/{???????}
    username: ${DB_USER} # MySQL ?? ??
    password: ${DB_PW} # MySQL ????

  jpa:
    database: mysql # ??? ?????? ?? ?? (MySQL)
    database-platform: org.hibernate.dialect.MySQLDialect # Hibernate?? ??? MySQL ??(dialect) ??
    show-sql: true  # ??? SQL ??? ??? ???? ?? ??
    hibernate:
      ddl-auto: create # ?????? ?? ? ?????? ???? ??? ??
    properties:
      hibernate:
        format_sql: true  # ???? SQL ??? ?? ?? ???